workflows:
  android-debug-build:
    name: GymTimer Debug APK
    max_build_duration: 30
    environment:
      java: 17
    scripts:
      - name: Build debug APK
        script: gradle assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
